#include <stdio.h>
// Die obige Zeile fügt die Standard Input/Output Bibliothek hinzu, die Funktionen für die Ein- und Ausgabe bereitstellt.

int main()
{
    // Die Funktion main() ist der Startpunkt des Programms. Hier beginnt die Ausführung.

    printf("Hello World");
    // Die printf-Funktion wird verwendet, um Text auf der Konsole auszugeben.
    // In diesem Fall wird der Text "Hello World" auf dem Bildschirm angezeigt.

    return 0;
    // Die Funktion main() gibt normalerweise eine Ganzzahl zurück, um anzuzeigen, ob das Programm erfolgreich ausgeführt wurde (0 für Erfolg).
}
